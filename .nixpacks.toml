[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.setuptools"]

[phases.install]
cmds = ["python3.11 -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3.11 -m gunicorn run:app"